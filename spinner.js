import React from "react";

const Spinner = () => {
    return (
        <div className="text-center">
            <img src="/path-to-your-spinner/loading.gif" alt="loading" />
        </div>
    );
};

export default Spinner;
